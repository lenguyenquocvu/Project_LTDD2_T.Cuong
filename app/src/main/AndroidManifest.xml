<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.quanlydiemsinhvien">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Vũ -->
        <activity android:name=".activities.DanhSachKhoaHocActivity" >
        </activity>
        <activity android:name=".activities.DanhSachChuongTrinhDaoTaoActivity"></activity>
        <activity android:name=".activities.DanhSachLopHPTheoMHActivity"></activity>

        <!-- Hiệu -->
        <activity android:name=".activities.KhoaActivity" ></activity>
        <activity android:name=".activities.NganhActivity"></activity>

        <!-- Hương -->
        <activity android:name=".activities.MainDanhSachSinhVien"></activity>
        <activity android:name=".activities.MainDanhSachGiangVien" ></activity>
        <activity android:name=".activities.QuanLyActivity">
        </activity>

        <!-- Tâm -->
        <activity android:name=".activities.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.DSSinhVienActivity"></activity>
        <activity android:name=".activities.MainDanhSachPhongDaoTao" ></activity>
        <activity android:name=".activities.MainDanhSachAdmin" ></activity>

        <!-- Triều -->
        <activity android:name=".activities.DSLopActivity" ></activity>
        <activity android:name=".activities.DanhSachGiangVienActivity"></activity>
        <activity android:name=".activities.DanhSachSinhVienActivity"></activity>

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />


    </application>

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <dist:module dist:instant="true" />

</manifest>