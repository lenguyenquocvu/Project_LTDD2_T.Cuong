<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/option_menu"
        android:icon="@mipmap/option_menu"
        android:title="@string/menuTitle"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/them_item"
                android:title="@string/themMenuItem" />
            <item
                android:id="@+id/thoat_item"
                android:title="@string/thoatMenuItem" />
        </menu>
    </item>

</menu>